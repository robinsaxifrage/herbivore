package herbivore.game;

/**
 * an enum representing the selected hub placement set up
 * loaded from the configuration file
 * @author herbivore
 */
public enum HudPlacementEnum {

    static_placement, dynamic_placement
    
}
